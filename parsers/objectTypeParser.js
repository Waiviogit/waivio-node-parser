const {ObjectType} = require('../models');
const {redisSetter} = require('../utilities/redis');

const parse = async (operation, metadata) => {
    lalalallalalalala lalalalalla lalal lala lal al;
};

module.exports = {parse};